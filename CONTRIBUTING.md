# Contributing

This repository uses Apache-2.0 and requires contributions to preserve the MAP
1.0 boundary: the CLI is a client, not the hosted control plane.

Before sending changes, run:

```sh
cargo test
```
